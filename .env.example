# Neon Database
# Sign up at https://neon.tech and create a database
# Get your connection string from the dashboard
DATABASE_URL=postgresql://user:password@host/database

# Authentication - Clerk
# 1. Sign up at https://clerk.dev
# 2. Create a new application
# 3. Go to API Keys page: https://dashboard.clerk.com/last-active?path=api-keys
# 4. Copy your Publishable Key and Secret Key
# 5. Replace the placeholder values below with your actual keys
CLERK_SECRET_KEY=YOUR_SECRET_KEY
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=YOUR_PUBLISHABLE_KEY

# You don't need to change these, just copy them over into your `.env` file.
NEXT_PUBLIC_CLERK_SIGN_IN_URL='/sign-in'
NEXT_PUBLIC_CLERK_SIGN_UP_URL='/sign-up'
NEXT_PUBLIC_CLERK_SIGN_IN_FORCE_REDIRECT_URL='/app'
NEXT_PUBLIC_CLERK_SIGN_UP_FORCE_REDIRECT_URL='/app'

# Upstash Redis
# Sign up at https://upstash.com and create a Redis database
# Get your REST URL and token from the dashboard
UPSTASH_REDIS_REST_URL=https://your-upstash-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token

# Resend - Email service
# Sign up at https://resend.com
# Get your API key from the dashboard
RESEND_API_KEY=re_your_resend_api_key

# Skip environment validation until all services are configured
# Uncomment this line during initial setup, remove once all keys are configured
# SKIP_ENV_VALIDATION=true
